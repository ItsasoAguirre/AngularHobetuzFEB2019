import { SeleccionarJugadoresPipe } from './seleccionar-jugadores.pipe';

describe('SeleccionarJugadoresPipe', () => {
  it('create an instance', () => {
    const pipe = new SeleccionarJugadoresPipe();
    expect(pipe).toBeTruthy();
  });
});
