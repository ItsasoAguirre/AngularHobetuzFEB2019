import { FiltroBuscarPipe } from './filtro-buscar.pipe';

describe('FiltroBuscarPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBuscarPipe();
    expect(pipe).toBeTruthy();
  });
});
