import { ImportantePipe } from './importante.pipe';

describe('ImportantePipe', () => {
  it('create an instance', () => {
    const pipe = new ImportantePipe();
    expect(pipe).toBeTruthy();
  });
});
